#set ($bean = $modelName) 
#set ($beanlen = $bean.length())
#set ($Bean = $bean.substring(0,1).toUpperCase()+$bean.substring(1,$beanlen)) 

package com.${domain}.dao.impl;

import java.io.Serializable;

import org.springframework.stereotype.Service;

import com.${domain}.model.$Bean;
import com.${domain}.dao.${Bean}Dao;

@Service("${bean}Dao")
public class ${Bean}DaoImpl extends HibernateDaoImpl<$Bean, Serializable> implements ${Bean}Dao {

}