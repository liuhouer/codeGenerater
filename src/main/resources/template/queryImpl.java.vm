#set ($bean = $modelName) 
#set ($beanlen = $bean.length())
#set ($Bean = $bean.substring(0,1).toUpperCase()+$bean.substring(1,$beanlen)) 

package ${suffix}.${domain}.query.impl;
import org.apache.commons.lang.StringUtils;
import org.springframework.stereotype.Service;
import ${suffix}.${domain}.query.${Bean}Query;
import ${suffix}.${domain}.query.condition.${Bean}QueryCondition;

/**
 * @author bruce
 * @date ${date}
 * @email zhangyang226@gmail.com
 * @site http://blog.northpark.cn | http://northpark.cn | orginazation https://github.com/jellyband
 * 
 */
 
@Service("${Bean}Query")
public class ${Bean}QueryImpl implements ${Bean}Query {

	@Override
	public String getSql(${Bean}QueryCondition ${Bean.toLowerCase()}QueryCondition) {
		StringBuilder sql = new StringBuilder(" where 1=1");
		
        
		return sql.toString();
	}
	
	@Override
	public String getMixSql(${Bean}QueryCondition condition) {
		// TODO Auto-generated method stub
		StringBuilder sql = new StringBuilder(" where 1=1");
		
        
		return sql.toString();
	}

}



